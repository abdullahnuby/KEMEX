# KEMEX Web — قائمة متابعة التنفيذ

> المرجع الوظيفي والتصميمي: ملف HTML الأصلي `TFMS_Fixed.html` المرفق بالمشروع. لا يُعتبر أي بند مكتملًا إلا بعد مراجعة الكود واختباره فعليًا.

## مفتاح الحالة
- [x] موجود في المصدر الحالي، لكن لا يعني وحده أنه اجتاز اختبارًا وظيفيًا شاملًا.
- [ ] لم يكتمل/يحتاج مراجعة أو تنفيذ.
- [!] فجوة مؤكدة ظهرت أثناء فحص المصدر.

## 1. تثبيت الأساس ومطابقة المرجع
- [x] تطبيق ويب React + TypeScript + Vite موجود.
- [x] إعداد دعم العربية وRTL موجود في المشروع.
- [x] مرجع HTML الأصلي محفوظ ضمن `legacy/TFMS_Fixed.html`.
- [x] استخراج مجموعات التابات الأساسية ومقارنتها بقائمة التنقل في React (Sprint 01؛ بقي تدقيق عناصر وأزرار كل صفحة).
- [!] تدقيق Sprint 01 كشف غياب عقود الإيجار من قائمة التنقل، وغياب صفحة تفاصيل الأصل المخصصة؛ راجع `docs/SPRINT-01-REFERENCE-AUDIT.md`.
- [ ] مراجعة بقية التنقل والترتيب والتجاوب والطباعة مقابل المرجع.
- [ ] مراجعة خريطة الأدوار والصلاحيات لكل صفحة ولكل إجراء.

## 2. الوظائف المشتركة
- [x] مكوّن عام لسجلات الوحدات يتضمن عرضًا وإضافة وتعديلًا وحذفًا وبحثًا وتصفية وتصدير CSV.
- [x] نماذج السجلات تستخدم تحقق الحقول المطلوبة والأنواع بحسب الإعدادات الموجودة.
- [ ] اختبار CRUD فعليًا على قاعدة البيانات لكل وحدة، بما يشمل حالات الفشل والتكرار.
- [ ] اختبار البحث والتصفية والتصدير والتفاصيل لكل وحدة.
- [ ] التحقق من العلاقات المرجعية بين الأصول والمشروعات والسائقين والعقود وغيرها.
- [ ] اختبار الصلاحيات من الواجهة ومن طبقة قاعدة البيانات/RLS.
- [ ] توحيد رسائل النجاح والفشل والتأكيدات وحالات التحميل عبر جميع الصفحات.

## 3. الصفحات والوحدات الموجودة
- [x] لوحة المعلومات — صفحة مستقلة موجودة؛ يلزم اختبار المؤشرات ومصادرها.
- [x] الأصول والأسطول — صفحة مستقلة موجودة؛ يلزم اختبار دورة العمل والحقول.
- [x] أوامر العمل/الصيانة — صفحة مستقلة موجودة؛ يلزم اختبار دورة العمل.
- [x] الوقود — صفحة مستقلة موجودة؛ يلزم اختبار الإدخال والحسابات.
- [x] المشروعات — صفحة مستقلة موجودة؛ يلزم اختبار الربط بالأصول.
- [x] التنبيهات — صفحة مستقلة موجودة؛ يلزم اختبار شروط التنبيه ومصادرها.
- [x] التقارير — صفحة مستقلة موجودة؛ يلزم مطابقة التقارير للمرجع والتحقق من الحسابات.
- [x] المستخدمون والصلاحيات — صفحة مستقلة موجودة؛ يلزم اختبار تحديث المستخدم والأدوار فعليًا.
- [x] الإعدادات العامة — صفحة مستقلة موجودة؛ يلزم اختبار حفظ الإعدادات وانعكاسها على النظام.
- [x] الوحدات العامة: طلبات المعدات، التخصيصات، التشغيل اليومي، الرحلات، السائقون، العقود، خطط الصيانة، الزيوت والفلاتر، الإطارات، المخازن، حركة المخزون، المشتريات، التكاليف والإهلاك، التحميل الداخلي، الفواتير والمستحقات، العملاء، سجل التدقيق.
- [!] هذه الوحدات العامة تعتمد حاليًا على `ModuleRecordsPage` وإعدادات حقول مشتركة؛ وجود النموذج العام لا يثبت اكتمال تدفقات العمل المتخصصة أو الاعتمادات والحسابات المطلوبة في المرجع.

## 4. فجوات مؤكدة من فحص المصدر الحالي
- [x] إزالة callbacks الفارغة `onSaved={()=>{}}` من صفحتي المستخدمين والإعدادات؛ الحفظ يُدار داخل الصفحتين ولا يحتاج callback غير مستخدم.
- [!] توجد صفحة `ModulePlaceholderPage` تعرض بوابة مؤقتة؛ يجب حصر المسارات التي تصل إليها وتحديد ما إذا كانت مطلوبة في المرجع، ثم استكمالها أو إزالة المسار غير المستخدم.
- [x] إزالة تجاهل خطأ التحقق من المستخدم البعيد في `App.tsx` واستبداله برسالة خطأ ظاهرة في واجهة التطبيق.
- [x] فصل تحميل بيانات الوحدات باستخدام نتائج مستقلة؛ فشل وحدة لا يمنع عرض بقية البيانات، وتظهر رسالة بأسماء الوحدات التي تعذر تحميلها.
- [ ] إضافة إجراء إعادة محاولة واضح للبيانات التي فشل تحميلها.
- [ ] مراجعة إغلاق الجلسة، انتهاء صلاحيتها، والتعامل مع مستخدم غير نشط.

## 5. التقارير والإدارة
- [ ] مطابقة كل تقرير مع المرجع، بما يشمل المرشحات والفترات الزمنية ومجاميع البيانات.
- [ ] اختبار تصدير التقارير ووضوح تنسيق العربية.
- [ ] التحقق من سجل التدقيق: تسجيل العمليات الحساسة، هوية المنفذ، الوقت، والمرجع.
- [ ] اختبار أن مستخدمًا لا يستطيع إنشاء سجل تدقيق باسم/معرّف مستخدم آخر، وأن التدفق المشروع لا يزال يعمل.
- [ ] التحقق من الإشعارات والتنبيهات: شروط التوليد، القراءة، وإعادة التحديث.
- [ ] التأكد من أن إعدادات الأسعار والتنبيه تؤثر فعلًا في الحسابات والتنبيهات التابعة لها.

## 6. الاختبار والتسليم
- [ ] تثبيت الحزم باستخدام إصدار Node المحدد بالمشروع.
- [ ] تشغيل `npm run typecheck` وإصلاح جميع الأخطاء.
- [ ] تشغيل `npm run build` وإصلاح جميع الأخطاء.
- [ ] اختبار تشغيل التطبيق محليًا مع إعدادات قاعدة البيانات.
- [ ] اختبار سيناريوهات المستخدم حسب كل دور.
- [ ] فحص كل زر ظاهر للتأكد من أن له إجراءً حقيقيًا أو أنه معطل مع توضيح السبب.
- [ ] فحص سلامة الأرشيف النهائي ومراجعة محتوياته.
- [ ] إصدار نسخة تسليم فقط بعد تسجيل نتائج الاختبار الفعلية.

## Sprint 06-01 — Operating workflow actions (v0.11.5)
- [x] تثبيت حالات دورة العمل للطلبات والتخصيصات والتشغيل والرحلات والمشتريات وفق حالات المرجع بدل السماح بتعديل الحالة يدويًا من النموذج.
- [x] إضافة أزرار تقديم/اعتماد/إعادة/رفض حسب الحالة والدور في طلبات المعدات.
- [x] إضافة إجراء إنهاء التخصيص، مع تحديث حالة الأصل وإغلاق الطلب المرتبط عند وجود الربط في البيانات.
- [x] إضافة تقديم/اعتماد/إعادة لسجلات التشغيل، واعتماد التشغيل يحدّث آخر عداد موثوق للأصل.
- [x] إضافة اعتماد الرحلات ومجموعة إجراءات الاعتماد/الرفض/إصدار أمر الشراء للمشتريات.
- [x] إنشاء حالات ابتدائية تلقائية للسجلات الجديدة وأرقام مرجعية أولية للطلبات/التخصيصات/الرحلات/المشتريات.
- [x] إضافة مسار إنشاء تخصيص مرتبط بطلب معتمد، مع اختيار أصل متاح وتسجيل التسليم وتحديث حالة الأصل والطلب.
- [x] ضبط صلاحيات الكتابة لهذه الوحدات لتقترب من مصفوفة الأدوار في المرجع الأصلي.
- [x] فحص المصدر `node scripts/verify-source.mjs` — ناجح بعد التعديلات.
- [!] `typecheck` و`build` لم يتم التحقق منهما؛ البيئة الحالية Node 22 والمشروع يتطلب Node 24+، كما انتهت محاولة `npm install` بمهلة.
- [ ] اختبار مسارات الاعتماد على Supabase فعليًا، بما في ذلك RLS و`approval_events`.
- [ ] استكمال التخصيص التفاعلي من طلب معتمد إلى اختيار أصل، ثم اختبار الترابط مع الأصل/المشروع.



## Sprint 06-02 — الصيانة الوقائية والمخزون (v0.11.6)
- [x] تحويل «خطط الصيانة» إلى صفحة متخصصة بحساب الاستحقاق حسب نوع العداد أو الأيام.
- [x] إنشاء أمر عمل من خطة صيانة مستحقة مع حفظ مرجع الخطة.
- [x] تحويل «أوامر العمل» إلى دورة حالة مرتبطة بأزرار الإجراءات: اعتماد → بدء → انتظار قطع غيار → إنجاز.
- [x] تسجيل تكاليف الإنجاز ونتائج الفحص عند إغلاق أمر العمل.
- [x] تحويل «المخازن وقطع الغيار» إلى صفحة متخصصة تعرض الرصيد والحد الأدنى وقيمة المخزون.
- [x] إضافة صرف مخزني مع منع تجاوز الرصيد وتسجيل حركة مرتبطة بالأصل/أمر العمل/المشروع.
- [x] إضافة إنشاء طلب شراء لأصناف تحت الحد الأدنى دون تنفيذ شراء مباشر.
- [x] إصلاح Mapping لبيانات Demo المحلية بين مفاتيح المصدر القديم وأسماء وحدات React.
- [x] `node scripts/verify-source.mjs` — ناجح بعد تحديث فحوص Sprint 06-02.
- [!] `typecheck` و`build` ما زالا غير متحققين بسبب مشكلة تثبيت الاعتماديات/إصدار Node.
- [ ] اختبار المخزون والصيانة فعليًا على Supabase/RLS.
- [ ] جعل حركة المخزون وتحديث الرصيد ذرية عبر transaction/RPC.
- [ ] اختبار المتصفح والتجاوب ضمن Sprint 08.


## Sprint 06-03 — الزيوت والإطارات والمشتريات المتخصصة (v0.11.7)
- [x] تحويل «الزيوت والفلاتر» إلى صفحة متخصصة تعرض خطط التغيير والاستحقاق حسب العداد/الساعات/الأيام.
- [x] تسجيل تغيير الزيت/الفلاتر مع الأصل والتاريخ والعداد والكمية وتكلفة المواد والعمالة والمنفذ وأمر العمل.
- [x] تحديث آخر تنفيذ في خطة الزيوت بعد تسجيل التغيير المرتبط بها.
- [x] تحويل «الإطارات» إلى صفحة متخصصة للحالة والتركيب والتدوير والفك والفحص والإصلاح والاستبعاد.
- [x] حفظ سجل مستقل لعمليات الإطارات وربطه بالإطار والأصل والتكلفة.
- [x] تحويل «المشتريات» إلى صفحة متخصصة مع مؤشرات وقائمة طلبات ودورة اعتماد.
- [x] إلزام إدخال المورد قبل إصدار أمر الشراء وإنشاء رقم PO فعلي.
- [x] محاذاة صلاحية المشتريات مع المرجع: admin / fleet / maint في Migration 006.
- [x] إضافة تحميل بيانات `oilChanges` و`tireOps` في التطبيق.
- [x] تحديث فحص سلامة المصدر ليتحقق من الصفحات والمسارات والبيانات الجديدة.
- [!] typecheck/build ما زالا غير متحققين بسبب الاعتماديات وبيئة Node.
- [ ] اختبار Supabase/RLS للوحدات الثلاث فعليًا.
- [ ] جعل تحديثات المخزون/السجلات المرتبطة ذرية عبر RPC/transaction حيث يلزم.

## Sprint 07-03 — الإدارة والمالية التشغيلية (v0.12.2)
- [x] تحويل «التكاليف والإهلاك» إلى شاشة مالية متخصصة تعتمد على الوقود والصيانة والزيوت والإطارات والإيجار والإهلاك.
- [x] تحويل «التحميل الداخلي» إلى شاشة متخصصة بمرجع تعريفة التحميل والحد الأدنى والفوترة حسب الاستخدام المعتمد.
- [x] تحويل «الفواتير والمستحقات» إلى دورة متخصصة: تسجيل ← مراجعة ← اعتماد ← ترحيل/سداد أو تحصيل.
- [x] تحويل «العملاء» إلى بطاقات تشغيلية تعرض بيانات الاتصال وعدد الفواتير والإيرادات المعتمدة، مع الإضافة والتعديل.
- [x] تحويل «سجل التدقيق» إلى شاشة قراءة محمية مع البحث والتصفية والتصدير.
- [x] تطبيق مصفوفة إظهار الوحدات حسب الدور في القائمة، مع حارس مسار داخل التطبيق لمنع الوصول عبر الرابط المباشر.
- [x] تحسين هوية منفذ سجل التدقيق في الوضع المحلي بحيث تتبع المستخدم الحالي بدل المستخدم الأول الثابت.
- [x] ربط إعدادات الأسعار والتنبيهات بمحركات الحساب والتنبيه المدعومة في المرجع (VAT/وقود/تنبيهات الأيام والعدادات والاستحقاقات).
- [x] مراجعة الحسابات المالية والتعريفات مقابل منطق المرجع: مسافة الفترة، الإهلاك المقيد بالعمر/الزمن الحالي، إيجار الوحدات، والتحميل بالحد الأدنى؛ الاختبار العملي مؤجل مع بقية الاختبارات.
- [ ] اختبار المستخدمين والصلاحيات وRLS على Supabase (مؤجل).
- [ ] typecheck/build واختبارات المتصفح (مؤجل).

## Sprint 07-02 — مركز التقارير المرجعي (v0.12.1)
- [x] توسيع مركز التقارير إلى 10 تقارير مطابقة لمجموعة التقارير في المرجع الأصلي.
- [x] إضافة مرشحات الفترة والفئة والملكية والحالة والمشروع.
- [x] إضافة تشغيل التقرير قبل التصدير.
- [x] إضافة تصدير CSV مع BOM مناسب للعربية والطباعة.
- [x] إدخال تقارير التكاليف والإهلاك والاستخدام والوقود والمخزون والموافقات ضمن مركز تقارير موحد.
- [ ] مطابقة حسابات التقارير مع كل المعادلات الأصلية اختبارًا عمليًا.
- [ ] اختبار التصدير والطباعة والمتصفح.
- [ ] typecheck/build (مؤجل).

## سجل التقدم
- **الحالة الحالية:** Sprint 08 تطويري مستمر؛ v0.20.0 يتضمن تشطيب واجهة Enterprise وتنظيفًا مركزيًا للمراجع. اختبارات المتصفح والبناء وSupabase/RLS ما زالت مؤجلة. أضيفت Migration 005 لتقييد نسبة سجل التدقيق إلى المستخدم المصادق عليه؛ لم تُطبّق أو تُختبر على Supabase حي بعد. أضيف تحقق حالة الحساب في تسجيل الدخول البعيد، وأضيفت منح Data API لجداول سير العمل في migration 004؛ لم تُختبر بعد على مشروع Supabase حي. عولجت نقطتان مؤكدتان في التطبيق (callbacks الفارغة وتجاهل خطأ جلسة المستخدم). توجد صفحات مستقلة ووحدات CRUD عامة، لكن لا يوجد حتى الآن إثبات اختبار شامل لكل التابات والإجراءات.
- **آخر فحص للمصدر:** أضيفت Migration 005 لسياسة إدخال سجل التدقيق. عولجت callbacks الفارغة وتجاهل خطأ جلسة المستخدم، وأضيف تحقق حالة المستخدم النشط عند الدخول البعيد ومنح Data API لجداول سير العمل. ما زالت صفحة بوابة مؤقتة موجودة، وتحتاج المسارات التي تصل إليها إلى حصر ومطابقة المرجع.
- **الخطوة الحالية:** Sprint 07 مكتمل من ناحية التطوير الوظيفي والواجهة. تبقى اختبارات التشغيل وSupabase/RLS وtypecheck/build والمتصفح مؤجلة بطلب المستخدم إلى مرحلة الاختبارات النهائية.

## سجل التغيير v0.10.0
- إضافة `005_audit_actor_policy.sql`: قصر إدخال سجل التدقيق على أن يكون `user_id` مساويًا لـ`auth.uid()`.
- تحديث قائمة المهام لتسجيل التعديل واختبار الأمان المطلوب.
- لم يتم تأكيد بناء التطبيق أو اختبار الاتصال بقاعدة Supabase حيّة.


## سجل التغيير v0.10.1
- تعديل تحميل البيانات في `App.tsx` لاستخدام `Promise.allSettled` بدل إيقاف تحميل كل الوحدات عند فشل طلب واحد.
- عرض البيانات المتاحة مع رسالة تفصيلية تحدد الوحدات التي تعذر تحميلها.
- تحديث رقم الإصدار إلى 0.10.1.
- لم يتم تأكيد البناء الكامل أو الاتصال بقاعدة Supabase فعلية؛ يلزم تثبيت الاعتماديات وتشغيل الاختبارات.


## Sprint 01 — Reference & Navigation Audit
- استخراج 25 عنصر تنقل أساسيًا من المرجع ضمن ست مجموعات ومقارنتها بقائمة React.
- فجوات مؤكدة: عقود الإيجار غير موجودة في القائمة الجانبية رغم وجود إعداداتها، ومسار بطاقة الأصل لا يملك صفحة تفاصيل React مخصصة.
- `npm run typecheck` غير صالح كتحقق للمشروع حاليًا بسبب غياب الاعتماديات؛ يلزم تثبيتها ثم إعادة الفحص.
- تقرير التفاصيل: `docs/SPRINT-01-REFERENCE-AUDIT.md`.

## Sprint 02 — Asset detail route
- [x] إنشاء صفحة بطاقة أصل مستقلة تتبع مسار `#/asset/:id`.
- [x] ربط زر العرض في جدول الأصول بصفحة البطاقة.
- [x] إظهار البيانات الأساسية للأصل وبيانات الملكية/العداد/المشروع/المستندات والقيم الرأسمالية المتاحة.
- [x] إضافة تبويبات البطاقة المطابقة لمجموعات المرجع: التخصيصات، التشغيل، الوقود، الصيانة، الإطارات، التكاليف، التدقيق.
- [x] ربط التبويبات بسجلات النظام المتاحة وإظهار حالة خلو السجلات عند عدم وجود بيانات.
- [x] توفير رجوع واضح لقائمة الأصول ووصلة إلى وحدة الصيانة.
- [!] التعديل من البطاقة يعيد المستخدم إلى قائمة الأصول؛ فتح نموذج تعديل الأصل مباشرة لم يكتمل بعد.
- [!] تبويبات الإطارات والتكاليف والتدقيق تعتمد على تطابق مفاتيح السجلات المتاحة؛ يلزم تدقيق الربط مع مخطط البيانات الفعلي.
- [ ] إكمال عقود الإيجار حسب نقاط الدخول الأصلية في المرجع (لا يضاف رابط جانبي افتراضي قبل تدقيق تلك النقاط).
- [ ] تثبيت الحزم وتشغيل typecheck/build واختبارات التفاعل؛ البيئة الحالية لا تحتوي على حزم React/Vite اللازمة.

## Sprint 03 — Common UI
- [x] مزامنة رقم الإصدار الظاهر مع الحزمة (0.11.0).
- [x] إضافة تسميات وصول لأزرار التنقل والتنبيهات وإجراءات الصفوف.
- [x] إعلان اختيار مرشحات الحالة باستخدام `aria-pressed`.
- [x] تنسيق حقول textarea ومؤشرات التركيز بلوحة المفاتيح.
- [x] تحسين تمرير الجداول وتوزيع أزرار الإجراءات على الهاتف.
- [ ] اختبار التفاعل فعليًا على أحجام شاشة متعددة.
- [ ] إعادة تشغيل typecheck/build بعد تثبيت الاعتماديات.
- [ ] اختبار الجداول والنماذج والتصدير في جميع الوحدات.
- تقرير السبرنت: `docs/SPRINT-03-COMMON-UI.md`.


## Sprint 04 — Data layer and delete audit (v0.11.2)
- [x] مراجعة مسارات القراءة/الحفظ في `TfmsRepository` ومراجعة migrations الموجودة.
- [x] تسجيل عمليات حذف السائقين والعقود والسجلات العامة في سجل التدقيق بعد نجاح الحذف.
- [x] منع حذف سجل التدقيق من خلال واجهة المستودع.
- [x] إظهار رسالة واضحة عند عدم وجود السجل أو عدم توفر صلاحية الحذف.
- [x] مزامنة رقم الإصدار في `package.json` و`src/config/app.ts` إلى 0.11.2.
- [x] `node scripts/verify-source.mjs` — ناجح.
- [ ] اختبار الحذف وسياسات RLS على Supabase حي/اختباري.
- [ ] `typecheck` و`build` — غير مكتملين بسبب عدم تثبيت الاعتماديات المطلوبة.
- [ ] معالجة عدم ذرّية الحذف والتدقيق؛ يتطلب الحل النهائي transaction/RPC بقاعدة البيانات.
- تقرير السبرنت: `docs/SPRINT-04-DATA-LAYER.md`.

## Sprint 05 — Authentication, roles and approvals (2026-09-21)
- ☑ Validate remote profile active state and supported role values on current-user lookup.
- ☑ Sign out remote sessions with inactive/unsupported profiles.
- ☑ Clear stale cached identity if remote validation returns no valid user or errors.
- ☑ Update version to 0.11.3.
- ⚠️ Client checks do not replace Supabase RLS authorization.
- ⚠️ Initial render may still use cached identity before asynchronous validation finishes.
- ☐ Gate application rendering on completed remote session validation.
- ☐ Test authentication and RLS against a real Supabase project.
- ☐ Implement/verify end-to-end approvals for requests, assignments, operations, and purchases.


## Sprint 05 follow-up — Session validation gate (v0.11.4)
- [x] Added a blocking session-validation state for remote mode so cached identity is not rendered as authenticated while `getCurrentUser()` is pending.
- [x] Keep local/demo mode immediately available without waiting for a remote check.
- [x] Display a validation loader until remote session lookup resolves, including error/no-profile outcomes.
- [x] Bumped package and application version to 0.11.4.
- [ ] Verify remote auth behavior against a configured Supabase project and browser scenarios.
- [ ] Install dependencies and run TypeScript/build; dependencies are currently absent in `node_modules`.


## Sprint 07-01 — التقارير المالية والإدارية (v0.12.0)
- [x] إضافة تقارير التكاليف والإهلاك، التحميل الداخلي، الفواتير والمستحقات، والمشتريات إلى مركز التقارير.
- [x] ربط التقارير بسجلات الوحدات المحملة فعليًا في `moduleData`.
- [x] تطبيق مرشح التاريخ على التقارير المالية الجديدة.
- [x] توفير تصدير CSV موحد مع BOM لدعم فتح العربية في برامج الجداول.
- [x] تمرير بيانات الوحدات من التطبيق إلى مركز التقارير.
- [x] تحديث رقم الإصدار إلى 0.12.0.
- [ ] مطابقة أعمدة ومجاميع كل تقرير تفصيليًا مع المرجع الأصلي.
- [ ] مراجعة احتساب الإهلاك والضريبة والتحميل مقابل قواعد العمل المرجعية؛ لا توجد معادلات محاسبية جديدة مفترضة في هذه الدفعة.
- [ ] اختبار التصفية والتصدير في المتصفح.
- [ ] اختبار الصلاحيات وبيانات Supabase الفعلية.
- [ ] تأجيل typecheck/build واختبارات التشغيل حسب توجيه المستخدم.


## Sprint 07-04 — ربط الإعدادات ومصفوفة الإدارة (v0.12.2)
- [x] تحميل إعدادات المؤسسة ضمن سياق التطبيق مع الوحدة المالية والإدارية.
- [x] جعل نافذة التنبيه الزمنية المعرفة في الإعدادات تؤثر في تنبيهات الأصول والسائقين.
- [x] جعل ضريبة القيمة المضافة المعرفة في الإعدادات تستخدم عند تسجيل الفاتورة الجديدة بدل رقم ثابت.
- [x] جعل أسعار الديزل والبنزين المعرفة في الإعدادات تظهر كقيمة ابتدائية لحركة الوقود الجديدة.
- [x] جعل تحديث الإعدادات ينعكس فورًا على التطبيق دون إعادة تسجيل الدخول.
- [x] تطبيق مصفوفة الظهور حسب الدور في القائمة وحارس المسار للرابط المباشر.
- [x] جعل سجل التدقيق المحلي يستخدم المستخدم الحالي كمنفذ عند توفره.
- [ ] فحص قواعد كل دور على Supabase/RLS فعليًا (مؤجل).
- [ ] typecheck/build واختبارات المتصفح (مؤجل).


## Sprint 07 — Development Complete (v0.13.0)
- [x] استكمال التقارير المالية والإدارية العشرة مع التصفية والتصدير والطباعة.
- [x] استكمال التكاليف والإهلاك، التحميل الداخلي، الفواتير والمستحقات، والعملاء وسجل التدقيق.
- [x] ربط إعدادات المؤسسة بمحركات الأسعار والضريبة والتنبيهات المدعومة.
- [x] استبدال Sidebar بـNavbar علوي احترافي بقوائم منسدلة ومتجاوب للموبايل.
- [x] إضافة عقود الإيجار إلى التنقل وفق مصفوفة الأدوار.
- [x] إنشاء Reference Resolver مركزي وعرض الاسم البشري أولًا والكود كمرجع ثانوي في الجداول والنماذج.
- [x] تصحيح عرض الأصول والمشروعات وأوامر العمل والطلبات والعملاء والعقود والمخزون والإطارات من المعرفات الخام إلى التسميات المفهومة عند توفر السجل.
- [x] مواءمة منطق التكلفة/الإهلاك/التحميل مع الدوال المرجعية حيث توجد قواعد صريحة في المصدر.
- [ ] اختبارات typecheck/build والمتصفح وSupabase/RLS — مؤجلة.


## Sprint 08-01 — Frontend Overhaul & Reference Display (v0.14.0)
- [x] اعتماد نظام بصري موحد للواجهة العربية RTL بخط أكثر ملاءمة وتدرج أحجام أوضح.
- [x] تحسين Navbar العلوي وإضافة أيقونات سياقية لقوائم الأقسام المنسدلة.
- [x] تحسين الأزرار والحقول والبطاقات والجداول والنوافذ المنبثقة وحالات التركيز.
- [x] إعادة تصميم مركز التقارير إلى مساحة عمل مخصصة بدل جدول واحد كثيف.
- [x] تقديم 10 تقارير أساسية كبطاقات اختيار واضحة مع وصف مختصر لكل تقرير.
- [x] إضافة لوحة مؤشرات التقرير ومرشحات أكثر وضوحًا وحالة جاهز/مسودة قبل التصدير.
- [x] ترتيب بيانات التقارير بحيث يظهر اسم الأصل أولًا والكود كمرجع ثانوي.
- [x] حل عرض الأصول/المشروعات كأكواد فقط في لوحة المعلومات ومساحات التقارير المعالجة في هذه الدفعة.
- [x] توسيع Reference Resolver لمرادفات asset/project/equipment/vehicle.
- [x] تقديم اسم الأصل/المشروع قبل الكود داخل قوائم الاختيار التشغيلية التي تم تدقيقها.
- [x] تحديث نسخة التطبيق والحزمة إلى 0.14.0.
- [x] `node scripts/verify-source.mjs` — ناجح بعد مزامنة الإصدار.
- [x] TypeScript syntax diagnostics على جميع ملفات TS/TSX — ناجح.
- [ ] `npm run typecheck` الكامل — مؤجل لمرحلة QA.
- [ ] `npm run build` — مؤجل لمرحلة QA.
- [ ] Browser visual regression على المقاسات المختلفة — مؤجل لمرحلة QA.
- [ ] Supabase/RLS end-to-end — مؤجل لمرحلة QA.


## Sprint 08-03 — UI consistency + reference hardening (v0.16.0)
- [x] ترقية typography واستخدام IBM Plex Sans Arabic كخط أساسي.
- [x] توحيد الجداول والحقول والأزرار والـdialogs والأيقونات بصريًا.
- [x] تحسين الـNavbar وقوائم الأقسام المنسدلة.
- [x] إعادة ضبط مركز التقارير بصريًا ورفع كثافة المعلومات بشكل مقروء.
- [x] إزالة تكرار عمود الكود في تقرير الأصول المملوكة؛ الاسم أولًا والكود داخل الخلية.
- [x] توسيع resolver لقبول asset/project IDs وcodes legacy في التقارير والتدفقات التي تم تدقيقها.
- [x] إصلاح عنوان صفحة تخصيص الأصل ليعرض اسم المشروع بدل المعرّف الخام.
- [x] تحويل ارتباط الفاتورة إلى اختيار مرجعي بأسماء الأصول والمشروعات والعقود والعملاء.
- [x] تحسين تنسيق Alert entity ليظهر الاسم أولًا.
- [x] تحديث package/app/verify version إلى 0.16.0.
- [ ] `typecheck` و`build` — مؤجلان.
- [ ] Browser visual regression — مؤجل.
- [ ] Supabase/RLS QA — مؤجل.


## Sprint 08-04 — Enterprise UI polish & exhaustive reference display (v0.17.0)
- [x] توحيد عرض المرجع بحيث تكون تسمية الأصل/المشروع أساسية والكود ثانويًا في الوحدات والجداول التي تمت مراجعتها.
- [x] دعم العلاقات القديمة التي تخزن `id` أو `code` في بطاقة الأصل، التكاليف، التحميل الداخلي، ولوحة المعلومات.
- [x] تحسين بطاقات المشروعات بحيث يكون اسم المشروع هو العنوان والكود/مركز التكلفة بيانات ثانوية واضحة.
- [x] تحسين أسطح الجداول والـhover والـsticky headers وأزرار الإجراءات والأيقونات.
- [x] رفع مستوى مركز التقارير من ناحية hierarchy والمرشحات وKPI ومساحة النتائج والطباعة.
- [x] إضافة تنسيق طباعة خاص بالتقارير بدون عناصر التنقل والفلاتر غير اللازمة للطباعة.
- [x] إضافة مرئيات أكثر وضوحًا لحالات empty states والمرجع الثانوي.
- [x] مزامنة الإصدار إلى 0.17.0.
- [x] فحص المصدر `node scripts/verify-source.mjs` — يجب إعادة تشغيله قبل الإصدار النهائي.
- [ ] typecheck/build وBrowser QA وSupabase/RLS — ما زالت مؤجلة.


## Sprint 08-05 — Final visual consistency + reference display pass (v0.18.0)
- [x] رفع مستوى typography والـspacing على مستوى النظام مع الحفاظ على العربية RTL.
- [x] توحيد كثافة الجداول والأزرار وإجراءات الصفوف لتقليل المظهر العام المتقطع بين الصفحات.
- [x] جعل أسماء الأصول/المشروعات هي العنصر البصري الأساسي في التفاصيل، مع إبقاء الكود كمرجع ثانوي.
- [x] تحسين بطاقة الأصل: الاسم أولًا، والكود ضمن هوية ثانوية واضحة.
- [x] تحسين شاشة الإطارات/الزيوت/الصيانة والمخططات التي تحتوي على روابط مرجعية بحيث لا يظهر ID خام كخيار واجهة أساسي عندما يتوفر اسم بشري.
- [x] مراجعة نمط إجراءات الجداول والأيقونات وإضافة حالات تفاعل أكثر وضوحًا.
- [x] رفع وضوح مركز التقارير Executive-style دون تغيير نطاق التقارير الوظيفي.
- [x] تحديث رقم الإصدار إلى 0.18.0.
- [ ] فحص متصفح شامل على كل المسارات والأحجام.
- [ ] typecheck/build النهائي.
- [ ] Supabase/RLS والسيناريوهات الحية.


## Sprint 08-06 — Detail hierarchy + remaining reference leak cleanup (v0.19.0)
- [x] تحسين عنوان تفاصيل السجل العام ليستخدم اسمًا/وصفًا/رقمًا بشريًا قبل المعرف الخام عند توفره.
- [x] تنظيف عنوان تعديل أوامر العمل ليعرض وصف الأمر أولًا والمعرف كمرجع ثانوي.
- [x] تثبيت مبدأ الاسم/الوصف أولًا والكود/المعرف ثانويًا في طبقة الـdetail المشتركة.
- [x] تحسين الـrow actions في الجداول لتبقى متناسقة بصريًا عند الجمع بين إجراءات workflow والأيقونات.
- [x] تحديث الإصدار إلى 0.19.0.
- [ ] Browser visual regression لجميع الصفحات والأحجام.
- [ ] typecheck/build النهائي.
- [ ] Supabase/RLS QA.


## Sprint 08-07 — Enterprise UI finish + global reference cleanup (v0.20.0)
- [x] Strengthen global reference matching for ID/code/object-shaped and legacy ambiguous references.
- [x] Dashboard latest work orders show human-readable asset and project names.
- [x] Generic detail views prefer human-readable reference labels in their headings.
- [x] Audit CSV export resolves known references to human-readable labels.
- [x] Remove duplicate `ReferenceValue` import in `AssetsPage`.
- [x] Apply enterprise visual finish to typography, tables, controls, modals, workflow actions, dashboard and reports.
- [x] Improve mobile touch targets and modal/action layout.
- [x] Source integrity verification passed after the changes.
- [ ] Full browser visual regression.
- [ ] typecheck/build and live Supabase/RLS QA.


## Sprint 08-08 — UI reference finish (v0.21.0)
- [x] ترقية typography والـvisual density إلى نظام عربي أكثر اتساقًا.
- [x] تحسين Navbar والجداول والنماذج والأزرار وحالات focus.
- [x] توحيد العرض البشري للمراجع مع إبقاء الأكواد كمراجع ثانوية.
- [x] تقوية مطابقة ID/code في الوقود والتحميل الداخلي والتقارير.
- [x] منع تصدير الكود وحده كهوية الأصل في تحميل البيانات.
- [!] typecheck/build/Browser QA/Supabase-RLS ما زالت مؤجلة لمرحلة QA.


## Sprint 08-09 — Navigation & Form System Upgrade (v0.22.0)
- [x] Remove duplicated alerts navigation item; keep bell as the single alerts entry point.
- [x] Expand generic module forms with sections and business fields.
- [x] Upgrade specialized forms for assets, projects, maintenance, fuel, oils, tires, inventory, purchases, invoices, customers, settings and users.
- [x] Add shared premium form layout and richer contextual help/placeholders.
- [x] Keep human-readable asset/project references as the primary display.
- [x] Source integrity verification passed.
- [ ] Full typecheck/build (deferred).
- [ ] Browser visual QA (deferred).
- [ ] Supabase/RLS QA (deferred).


## Sprint 08 — Branding v0.23.0
- [x] تغيير الهوية الظاهرة إلى KEMEX.
- [x] تحديث عنوان المتصفح ووصف التطبيق.
- [x] تحديث اسم الحزمة وCI.
- [x] تحديث أسماء ملفات التصدير الجديدة إلى KEMEX.
- [x] الإبقاء على مفاتيح `tfms_*` التقنية لحماية التوافق مع البيانات والـlegacy.
- [ ] QA/Build النهائي مؤجل.
